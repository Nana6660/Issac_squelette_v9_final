package gameobjects;

import libraries.Vector2;

/**
 *
 */
public class BloodOfTheMartyr extends Equipement implements Actif{

    /**
     * @param position
     * @param size
     * @param imagePath
     */
    public BloodOfTheMartyr(Vector2 position, Vector2 size, String imagePath) {
        super(position, size, imagePath);
    }

}
