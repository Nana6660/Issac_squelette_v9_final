package gameobjects;

import libraries.Vector2;

/**
 *
 */
public class Pique extends Obstacle{

    /**
     * @param position
     * @param size
     * @param imagePath
     */
    public Pique(Vector2 position, Vector2 size, String imagePath) {
        super(position, size, imagePath);
    }
}
